# 🚀 快速部署指南

## 一、环境检查

```bash
# 运行部署检查脚本
python3 deploy_check.py
```

## 二、安装依赖

```bash
cd /www/wwwroot/154.201.68.178:5051
pip3 install -r requirements.txt
```

## 三、配置修改

编辑 `a.py` 文件，修改以下配置：

```python
# 第 33-35 行
API_ID = 你的API_ID
API_HASH = '你的API_HASH'
BOT_TOKEN = '你的BOT_TOKEN'

# 第 38 行
ADMIN_IDS = [你的Telegram用户ID]

# 第 429 行（如需要代理）
USE_PROXY = False  # 或 True
```

## 四、启动服务

```bash
# 方式1：使用脚本
./start.sh

# 方式2：使用 systemd
sudo systemctl start telegram-bot
```

## 五、访问后台

- 地址：`http://你的IP:5051` 或 `http://你的域名:5051`
- 默认账号：`admin` / `admin`
- **⚠️ 首次登录后立即修改密码！**

## 六、常用命令

```bash
# 启动
./start.sh

# 停止
./stop.sh

# 查看日志
tail -f bot.log

# 查看进程
ps aux | grep "python3 a.py"

# 检查端口
netstat -tlnp | grep 5051
```

## 七、防火墙配置

```bash
# Ubuntu/Debian
sudo ufw allow 5051/tcp

# CentOS/RHEL
sudo firewall-cmd --add-port=5051/tcp --permanent
sudo firewall-cmd --reload
```

## 八、systemd 服务配置

创建 `/etc/systemd/system/telegram-bot.service`：

```ini
[Unit]
Description=Telegram Fission Bot
After=network.target

[Service]
Type=simple
User=你的用户名
WorkingDirectory=/www/wwwroot/154.201.68.178:5051
ExecStart=/usr/bin/python3 /www/wwwroot/154.201.68.178:5051/a.py
Restart=always
RestartSec=10

[Install]
WantedBy=multi-user.target
```

启用服务：

```bash
sudo systemctl daemon-reload
sudo systemctl enable telegram-bot
sudo systemctl start telegram-bot
```

## 九、备份数据库

```bash
# 手动备份
cp bot.db bot_$(date +%Y%m%d).db

# 自动备份（添加到 crontab）
0 2 * * * cp /www/wwwroot/154.201.68.178:5051/bot.db /backup/bot_$(date +\%Y\%m\%d).db
```

## 十、常见问题

1. **机器人无法启动** → 检查网络和代理配置
2. **后台无法访问** → 检查防火墙和端口
3. **数据库锁定** → 确保只有一个进程运行
4. **依赖安装失败** → 使用国内镜像源

详细说明请查看：**部署指南.md**
